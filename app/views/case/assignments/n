= boot_page_title

- if @order_forms.empty?
  = link_to t_action(:new, OrderForm), new_order_form_path
- else
  Es gibt #{@order_forms.count} Auftragsformulare.
  = simple_form_for 'case', edit_case_assignment_path, :method => :get do |f|
    .row-fluid
      .span6
        = f.input 'intra_day_id', :input_html => {'data-autofocus' => true}
        = f.button :submit
      .span6
        = image_tag inline_order_form_url(@order_forms.first, :type => :overview), :size => "500x156"
