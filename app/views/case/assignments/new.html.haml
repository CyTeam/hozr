= boot_page_title

%h3 Barcodes drucken
#label-button
  = link_to 'Formular anzeigen', case_label_new_path, :remote => true, :class => 'btn'
#label-form

%h3 Formulare zum Scannen
#cases-for-scanning
  = render 'cases_for_scanning', :collection => Case.for_scanning

.form-horizontal
  .form-actions
    = link_to t_action(:scan), scan_order_forms_path, :method => :post, :class => 'btn btn-primary'
    = link_to 'Manuell erfassen', new_order_form_path, :class => 'btn'

- unless @order_forms.empty?
  %h3 Formulare zum Zuweisen
  = render 'form'
