- for column in Classification.content_columns
  %p
    %b
      = column.human_name
      \:
    = h @classification.send(column.name)
= link_to 'Edit', :action => 'edit', :id => @classification
|
\#{link_to 'Back', :action => 'list'}
