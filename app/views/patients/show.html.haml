.contextual
  = icon_link_to :update, '#', :type => 'primary', :icon => 'download-alt', :id => 'update-button'
  = contextual_link_to :delete, nil, :type => 'danger'
  = contextual_link_to :index
  = icon_link_to :invoice_overview, cydoc_url_for("patients/#{@patient.id}?tab=invoices"), :icon => 'list', :target => cydoc_hostname

= boot_page_title

= render 'form'
= render :partial => 'doctors/show', :object => @patient.doctor

- content_for :sidebar do
  = render :partial => 'cases/compact_list', :object => @patient.cases
