= contextual_links :index, Patient

= boot_page_title(number_with_delimiter(@patients.total_entries) + " Patienten")

= paginate @patients

= render 'patients/list', :collection => @patients

= paginate @patients
