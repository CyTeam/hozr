.phone_number
  - for @phone_number in vcard.contacts.select { |nr| !nr.number.nil? && !nr.number.strip.empty? }
    %span.phone_number_type= image_tag(@phone_number.phone_number_type + ".png", :style => 'vertical-align: middle') unless @phone_number.phone_number_type.nil?
    = h @phone_number.number
    %br/
  - end unless vcard.nil?
