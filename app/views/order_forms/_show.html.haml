= image_tag inline_order_form_path(@order_form, :type => :overview), 'data-crop' => true

:javascript
  $(document).ready(function() {
    $('img[data-crop]').Jcrop(
      onSelect: updateCropCoords
    );
  })

