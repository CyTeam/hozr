<h1>Listing insurances</h1>

<table class="listing">
  <tr>
    <th>Name</th>
    <th>Adresse</th>
    <th>Zu Handen</th>
  </tr>
<% for insurance in @insurances %>
  <tr onmouseover="showActions(<%= insurance.id %>)" onmouseout="hideActions(<%= insurance.id %>)">
    <td>
      <%= link_to h(insurance.name), :action => 'show', :id => insurance %>
    </td>
    <td style="height: 32px; width: 50%">
      <span style="float: left">
        <%=h insurance.street_address %>, <%=h insurance.postal_code %> <%=h insurance.locality %>; <%=h "Tel: #{insurance.phone_number}" unless insurance.phone_number == '' %>
      </span>
      <span id="actions_<%= insurance.id %>" style="display:none; float: right">
        <%= link_to image_tag("edit.png"), :action => 'edit', :id => insurance %>
        <%= link_to image_tag("remove.png"), { :action => 'destroy', :id => insurance }, :confirm => 'Wirklich löschen?' %>
      </span>
    </td>
    <td><%=h insurance.extended_address %>
  </tr>
<% end %>
</table>

<%= will_paginate @insurances %>

<br />

<%= link_to 'New insurance', :action => 'new' %>
