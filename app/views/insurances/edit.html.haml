%h1 Editing insurance
= form_tag :action => 'update', :id => @insurance  do
  = render :partial => 'form'
  = submit_tag 'Edit'
= link_to 'Show', :action => 'show', :id => @insurance
|
\#{link_to 'Back', :action => 'list'}
