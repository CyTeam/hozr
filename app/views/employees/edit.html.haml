%h1 Editing employee
= form_tag :action => 'update', :id => @employee  do
  = render :partial => 'form'
  = submit_tag 'Edit'
= link_to 'Show', :action => 'show', :id => @employee
|
\#{link_to 'Back', :action => 'list'}
