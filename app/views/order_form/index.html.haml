%h1 PAP Zettel mit Arztadressen beschriften
= form_tag(print_order_form_path) do
  %table
    %tr
      %th Arzt
      %td= select 'order_form', 'doctor_id', doctors_collection
    %tr
      %th Anzahl
      %td= text_field 'order_form', 'count'
  = submit_tag 'Drucken'
