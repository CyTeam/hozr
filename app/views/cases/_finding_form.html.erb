<% quality_findings = @case.classification.code == '1' ? ['a', 'b'] : ['a', 'b', 'c'] %>

<div id="quality_findings">
  <% for finding_class_code in quality_findings %>
    <% finding_class = FindingClass.find_by_code(finding_class_code) %>
    <%= link_to_add_finding_class(@case, finding_class) %>
  <% end %>
<div>

<div id="top_findings">
  <% for finding_class in TopFindingClass.find_all_by_classification_id(@case.classification_id) %>
    <%= link_to_add_finding_class(@case, finding_class.finding_class) %>
  <% end %>
</div>

<%= form_tag add_finding_case_path(@case), :id => "finding_classes", :after => "$('finding_class_selection').value = ''; $('finding_class_code').value = ''; $('finding_class_code').focus() "  do %>
  <label for="case_praxistar_eingangsnr">Text:</label>
  <%= text_field_with_auto_complete :finding_class, :selection, {:tabindex => 1}, :frequency => 0.05 %>
  <label for="case_praxistar_eingangsnr">Code:</label>
  <%= text_field :finding_class, :code, :size => 5, :tabindex => 2 %>
  <%= submit_tag "Los" %>
<% end %>
